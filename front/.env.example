# 复制为 .env.development.local 使用（*.local 已在仓库 .gitignore）
#
# 仅当你通过「域名 + nginx/caddy 反代」访问本机 Vite（npm run dev）时需要：
# 填浏览器里的完整来源（协议 + 域名，勿尾斜杠）。用于 HMR 与开发期模块 URL。
# 若仍出现「MIME type text/html」：检查 nginx 是否把 /@vite、/src 等请求错误地回退成了 index.html，
# 应对 location / 整段 proxy_pass 到 Vite 端口，不要用 try_files … /index.html。
# VITE_DEV_PUBLIC_ORIGIN=https://huijin.ikun.center
